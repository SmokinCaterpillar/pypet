name: test-test-test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  MY_VAR: One

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env:
          - MY_VAR: Two
          - MY_VAR: Three
    steps:
      - run: echo "$MY_VAR2"


